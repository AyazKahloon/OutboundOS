// Analytics: sent / opened / replied. TODO: aggregate Lead statuses + Resend webhook events.
export default function AnalyticsPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Analytics</h1>
      <p className="mt-2 text-gray-600">TODO: sent / opened / replied metrics.</p>
    </section>
  );
}
